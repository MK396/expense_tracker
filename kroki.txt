# 1. Stwórz folder projektu i wejdź do niego
mkdir expense_project && cd expense_project

# 2. Stwórz wirtualne środowisko (izolacja bibliotek)
python -m venv venv

# 3. Aktywuj środowisko
# Na Windows:
venv\Scripts\activate
# Na Mac/Linux:
source venv/bin/activate

# 4. Zainstaluj Django i Django REST Framework
pip install django djangorestframework django-cors-headers

# Stwórz projekt o nazwie 'core'
django-admin startproject core .

# Stwórz aplikację o nazwie 'expenses'
python manage.py startapp expenses

# Dodanie modułów do listy INSTALLED_APPS
INSTALLED_APPS = [
    # ... domyślne aplikacje django ...
    'rest_framework',
    'corsheaders',
    'expenses',
]
MIDDLEWARE = [
    'corsheaders.middleware.CorsMiddleware', # To dodajemy
    'django.middleware.common.CommonMiddleware',
    # ... reszta ...
]

# Dodaj też na dole pliku, aby Vue mogło się połączyć:
CORS_ALLOWED_ORIGINS = [
    "http://localhost:5173",
]

# Dodanie modelu bazy danych

#MIGRACJE
# Przygotuj instrukcje dla bazy
python manage.py makemigrations

# Wykonaj zmiany w bazie
python manage.py migrate

# Stwórz konto administratora, żebyś mógł dodawać wydatki ręcznie
python manage.py createsuperuser

# zarejestrowac model w adminie
from django.contrib import admin
from .models import Expense

admin.site.register(Expense)

# utworzenie serializerów
from rest_framework import serializers
from .models import Expense

class ExpenseSerializer(serializers.ModelSerializer):
    class Meta:
        model = Expense
        # fields = '__all__'
        fields = ['id', 'name', 'amount', 'category', 'date']

# utworzenie widokow
from django.shortcuts import render
from rest_framework import generics
from .models import Expense
from .serializers import ExpenseSerializer

class ExpenseListCreateView(generics.ListCreateAPIView):
    queryset = Expense.objects.all().order_by('-date')
    serializer_class = ExpenseSerializer

# tworzenie urls.py
from django.urls import path
from .views import ExpenseListCreateView

urlpatterns = [
    path('expenses/', ExpenseListCreateView.as_view(), name='expense-list'),
]

# następnie zarejestrowac w glownym pliku urls.py



----------------------------------------------------------------------
                    PODSUMOWANIE
1. Baza danych i Modele
    - stworzenie szablonów jak ma wygladac wydatek
2. Zarejestrowanie modelu w panelu admina
3. Serializery
    - tlumaczenie elementów w bazie danych na JSON
4. Utworzenie widoku
5. Utworzenie urls i zmapowanie ich
----------------------------------------------------------------------

# instalacja Vue
npm create vite@latest expense-frontend -- --template vue

# instalacja axios do rozmawiania z API w DJango
cd expense-frontend
npm install
npm install axios  # Biblioteka do "rozmawiania" z Twoim API w Django

# serwer deweloperski Vue
npm run dev